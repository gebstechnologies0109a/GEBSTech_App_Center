name: Deploy to Forge

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Forge deployment
        run: curl -fsS -X POST "${{ secrets.FORGE_DEPLOY_HOOK }}"
